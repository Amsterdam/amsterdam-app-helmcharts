apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amsterdam-app-contact.fullname" . }}-test-connection"
  labels:
    {{- include "amsterdam-app-contact.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amsterdam-app-contact.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
